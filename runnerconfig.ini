# YARN Configuration
QUEUE_NAME=default
DISTCP_BANDWIDTH_MB=100
DISTCP_NUM_MAPS=20
DISTCP_MEMORY_MB=4096

# Directory Configuration
HCFS_BASE_DIR=/tmp/hms-mirror-distcp
CHECKPOINT_DIR=/data/migration/checkpoint

# Kerberos Configuration
KEYTAB_PATH=/tmp/hive.keytab
PRINCIPAL=hive/vvs-baseiq-1.vpc.cloudera.com@VPC.CLOUDERA.COM
# Number of tables per chunk (default: 100)
# Increase this for better performance with smaller tables
# Decrease this if you have very large tables or memory constraints
CHUNK_SIZE=2
